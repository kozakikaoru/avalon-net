/ TODO: form_withにする
= form_for @organizer do |f|
  ==  render 'application/validation_error', object: @organizer

  .row
    .col.s12.input-field
      = f.text_field :name
      = f.label :name

  = f.submit '登録', class: submit_classes
